package pl.coderslab.mystore.page;

import org.openqa.selenium.WebDriver;

public class AddAddressPage {
    private WebDriver driver;

    AddAddressPage(WebDriver driver) {
        this.driver = driver;
    }
}
